pipeline {
    agent any
    stages {
        stage('Build') {
            steps { echo 'Compilando...' }
        }
        stage('Test') {
            steps { echo 'Ejecutando pruebas...' }
        }
        stage('Analyze') {
            steps { echo 'Analizando codigo...' }
        }
        stage('Deploy') {
            steps { echo 'Desplegando...' }
        }
    }
}

